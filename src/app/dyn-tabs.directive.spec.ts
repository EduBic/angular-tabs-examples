import { DynTabsDirective } from './dyn-tabs.directive';

describe('DynTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new DynTabsDirective();
    expect(directive).toBeTruthy();
  });
});
